# pkl-jellyfin

Typed Pkl module for rendering Jellyfin Docker Compose deployments.

It models the image, ports, config and cache directories, media mounts, restart policy, and optional hardware acceleration. Host paths and media remain deployment-specific values in the consuming host repository.
